/**
 * 元调控层类型定义 — 薄适配器，re-export kernel 领域类型
 */

export type { ConflictKind, ConflictSeverity, Conflict } from '@chrono/kernel';
export type { AllocationStrategy, ResourceAllocation } from '@chrono/kernel';
export type { IntegrationProposal } from '@chrono/kernel';
