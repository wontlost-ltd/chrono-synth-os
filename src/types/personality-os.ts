/**
 * P-OS 五层人格模型类型定义 — 薄适配器，re-export kernel 领域类型
 */

export type { SurvivalAnchorKind, SurvivalAnchor } from '@chrono/kernel';
export type { DecisionStyle } from '@chrono/kernel';
export type { CognitiveModel } from '@chrono/kernel';
export type { PersonaOSState } from '@chrono/kernel';
